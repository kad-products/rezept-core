# Docs Videos

- **Format:** we're going with 720p for now to have good enough quality to hopefully read the words in the demos but keep the file size down
- **Extension:** Quicktime uses `.mov` but since it uses the `h.264` codec it seems as though you can literally just change the file extension to `.mp4` and it becomes an `mp4` file.  